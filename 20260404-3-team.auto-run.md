# `20260404-3-team.auto-run.md`

## Goal

- 把 Team 模块当前**仍然值得继续做**的剩余优化收成一轮可发布的实现：补齐 observability、压下一层热点性能、推进剩余安全结构拆分、增强 sync conflict 策略，并通过代码验证与必要的 `.75 / .74` 运行态复核。

## Context

- 仓库 / 工作目录：
  - `/Users/haoniu/sh18/hao.news2/haonews`
- 参考输入：
  - [runbook-template.auto-run.md](/Users/haoniu/sh18/hao.news2/runbook-template.auto-run.md)
  - [20260404-Team-claude.md](/Users/haoniu/sh18/hao.news2/haonews/20260404-Team-claude.md)
  - [20260404-Team-claude-codex-task.auto-run.md](/Users/haoniu/sh18/hao.news2/haonews/20260404-Team-claude-codex-task.auto-run.md)
  - [20260404-team-webhook-pro1.auto-run.md](/Users/haoniu/sh18/hao.news2/haonews/20260404-team-webhook-pro1.auto-run.md)
  - [20260404-team-legacy-remove-pro1.auto-run.md](/Users/haoniu/sh18/hao.news2/haonews/20260404-team-legacy-remove-pro1.auto-run.md)
  - [20260404-team-live-pro1.auto-run.md](/Users/haoniu/sh18/hao.news2/haonews/20260404-team-live-pro1.auto-run.md)
- 当前已完成，不要重复做：
  - Team workspace / archive / SSE / A2A / webhook / P2P / 第一层复制治理
  - Team webhook delivery ledger / replay / status API
  - Team Store `ctx` 主入口、compat 收窄、task/artifact `index-first`
  - `handler.go` 主拆分、`store.go` 第一轮低耦合拆分
  - Team / Live `.75 / .74` 运行态基线、`v0.5.74` 发布
- 当前剩余高价值条目，来自原审阅文档但尚未完整收口：
  - `2.4 ListTeams` 第二层性能
  - `8.1` Team 日志
  - `8.2` Team metrics
  - `9.1` sync 热路径微优化
  - `9.2` conflict 合并第二层
  - `10.1` 模板数据结构过于扁平
  - `1.1` / `1.2` 的后续安全拆分余量
- 当前明确不要在本 runbook 里做：
  - 破坏式公共 API 改名
  - CRDT / 强一致性重构
  - 新外部依赖或数据库迁移
  - 重做已稳定的 Live 主线

## Execution Contract

- 读完整个文档后立即开始执行。
- 在本 runbook 完成或确认硬阻塞之前，不要向用户提问。
- 遇到小问题、小缺口、小歧义时，直接做最合理的低风险判断并继续。
- 如果存在多条可行路径，优先选择：
  - 风险更低的路径
  - 更可回滚的路径
  - 更符合当前代码和文档现状的路径
- 只有在真实硬阻塞时才允许停止。
- 如果出现硬阻塞：
  - 先尝试本文件中已有 fallback
  - 再尝试本地可验证的合理 fallback
  - 如果仍无法继续，把阻塞信息、已尝试动作、当前结论、精确下一步写回本文档，然后停止
- 执行过程中持续更新 checklist 状态，不要只在最后统一回填。

## Planning Rules

- 按依赖和关键路径排序，不按叙事顺序排序。
- 每一步都要可执行、可验证、可回填。
- 先做能降低排障成本和长期维护成本的事项。
- 优先小步推进真实收益，不为了“文件更好看”做大搬家。
- 只有在前面验证表明收益明确时，才进入更深一层拆分或运行态复核。

## Parallelization Plan

- 并行批次 A：
  - Team logging
  - Team metrics
- 并行批次 B：
  - ListTeams / summary 热点复核
  - sync 热路径微优化
- 并行批次 C：
  - conflict merge 第二层
  - 模板/view-model 收口
- 并行批次 D：
  - 文档写回
  - 运行态复核
  - `main + tag + release`

## Execution Plan

### Phase 1. Inspect Remaining Baseline

- [x] 读取最小必要上下文：
  - `internal/haonews/team/`
  - `internal/plugins/haonewsteam/`
  - `internal/haonews/team_sync.go`
  - 相关测试与模板
- [x] 明确本 runbook 的真实剩余面：
  - 哪些建议已完成，不再进入本轮
  - 哪些建议仍值得做
  - 哪些建议要明确 `defer`
- [x] 把以下条目分类写回本文档：
  - `2.4 ListTeams`：`not-needed`
    - 当前 `ListTeamsCtx` 已有限并发读取 team metadata，未再发现值得为本轮单独扩张的热点。
  - `8.1 logging`：`done`
    - 已补 Team 结构化异常日志，覆盖 JSONL 损坏、webhook fail/retry/dead-letter。
  - `8.2 metrics`：`done`
    - 复用已有 Team sync status，并把 webhook delivery status 接到 Team sync API / page。
  - `9.1 TeamSyncMessage.Key 微优化`：`done`
    - 已收口重复标准化逻辑，并补 benchmark。
  - `9.2 conflict merge 第二层`：`done`
    - 已补 conflict class / action matrix / subject label / webhook status 联动。
  - `10.1 模板数据结构`：`done`
    - `team_sync.html` 已切到 view-model 结构，不再靠扁平散字段堆页面。

### Phase 2. Observability First

- [x] 为 Team Store / webhook / sync / conflict 主路径补最小结构化日志：
  - webhook delivery fail / dead-letter / replay
  - sync apply / skip / conflict / prune
  - archive create failure
  - store 关键 JSONL / index 读取损坏
- [x] 日志要求：
  - 不刷屏
  - 带 teamID / objectID / conflict key / deliveryID
  - 只在异常或状态转换时打
- [x] 为 Team runtime 补最小 metrics 或等价 counters：
  - sync applied / skipped / conflicts / retries
  - webhook delivered / failed / dead-letter / replayed
  - archive create success / failure
- [x] 选择当前代码风格下最小接入点：
  - 优先复用已有 status / ops 输出
  - 只有在已有 metrics 面板不足时才新增导出字段

### Phase 3. Hot Path Second Layer

- [x] 复核 `ListTeams` 当前实现和真实热点：
  - 如果仍串行读重，做有限并发 + 清晰错误收敛
  - 如果当前已经足够，明确写回 `not-needed`
- [x] 复核频道摘要/团队摘要是否还存在大规模全量读：
  - 只在仍有真实热点时补 metadata/summary 复用
- [x] 对 sync 热路径做最小微优化：
  - 收口 `TeamSyncMessage.Key()` 重复标准化逻辑
  - 避免热点路径重复 `strings.ToLower(strings.TrimSpace(...))`
  - 不为微优化重写 sync 协议
- [x] 如有收益，补一个小 benchmark 或至少 focused direct proof

### Phase 4. Conflict Merge Second Layer

- [x] 核对当前 conflict reason / suggested_action / allow_accept_remote / keep_local 现状
- [x] 对这些对象推进第二层合并或动作分流：
  - `task`
  - `artifact`
  - `member`
  - `policy`
  - `channel`
- [x] 明确三类场景：
  - `remote_newer`
  - `local_newer`
  - `same_version_diverged`
- [x] 对安全对象补更稳定的处理路径：
  - 自动建议
  - `accept_remote`
  - `keep_local`
- [x] 对危险对象继续拒绝，但 reason / action hint 更清楚
- [x] 保证同步落到：
  - sync API
  - sync 页面
  - history 记录

### Phase 5. View-Model And File Split Finish Pass

- [x] 收口模板数据结构，不再继续把过多扁平字段直接塞页面：
  - 先从 `team_sync.html`
  - 再看 Team detail / history 是否有同类问题
- [x] 优先补 view model 结构，而不是大改模板表现
- [x] 若成本可控，继续第二轮低风险拆分：
  - `types.go`
  - `view_models.go` 或等价文件
  - 明确把大型模板 page-data 从 handler 里抽出
- [x] 不做高耦合 CRUD 再拆分，除非前面步骤已经自然触到

### Phase 6. Verify

- [x] 运行最强可行验证：
  - `go test ./internal/haonews/team -count=1`
  - `go test ./internal/plugins/haonewsteam -count=1`
  - `go test ./internal/haonews -run 'TestTeamPubSubRuntime|TestWriteTeamSyncState|TestTeam.*Conflict|TestTeam.*Ack' -count=1`
  - `go test ./internal/plugins/haonewsops -run 'TestPluginBuildServesBootstrapExplainAPI' -count=1`
  - `go build ./cmd/haonews`
- [x] 若改动触及 `.75 / .74` 可见运行态，再做最小运行态复核：
  - Team sync health / conflicts
  - Team webhook status / replay
- [x] 若验证失败，修复后重跑，而不是只记录失败

### Phase 7. Close Out

- [x] 更新本文档中的完成状态。
- [x] 写明最终结果：
  - 完成了什么
  - 明确 `defer` 了什么
  - 是否存在残余风险
- [ ] 如本轮形成正式发布批次，继续：
  - `main`
  - `tag`
  - `release`

## Verification

- 首选验证命令：
  - `go test ./internal/haonews/team -count=1`
  - `go test ./internal/plugins/haonewsteam -count=1`
  - `go test ./internal/haonews -run 'TestTeamPubSubRuntime|TestWriteTeamSyncState|TestTeam.*Conflict|TestTeam.*Ack' -count=1`
  - `go test ./internal/plugins/haonewsops -run 'TestPluginBuildServesBootstrapExplainAPI' -count=1`
  - `go build ./cmd/haonews`
- 备用验证方式：
  - `curl -s http://127.0.0.1:51818/api/teams/archive-demo/sync | python3 -m json.tool`
  - `curl -s http://127.0.0.1:51818/api/teams/runtime-webhook-team/webhooks/status | python3 -m json.tool`
- 完成标准：
  - [ ] 剩余高价值 Team 条目已收口成 `done / defer / not-needed`
  - [ ] 至少完成一轮 observability 增强
  - [ ] 至少完成一轮热点性能/热路径优化
  - [ ] conflict merge 第二层已落到 API / 页面 / history
  - [ ] 关键路径验证已执行
  - [ ] 没有已知未写回的硬阻塞
  - [ ] 最终汇报包含 `Completed / Blocked / Next Step`

## Fallback Rules

- 默认优先：
  - 现有 status / ops 输出
  - 现有 Team sync / webhook 测试
  - 现有模板和 view-model 风格
  - 最小改动
- 如果需要假设，优先采用：
  - 与现有 Team runtime 语义一致的假设
  - 与现有 `.75 / .74` 运维文档一致的假设
  - 可逆、低破坏性的假设
- 不要采用：
  - 无基准支撑的激进重写
  - 会覆盖用户工作区其他无关改动的整理
  - 引入新的外部依赖来解决当前局部问题

## Blockers / Resume

- 硬阻塞定义：
  - 缺失必须的权限 / 密钥 / 外部访问
  - Team 运行态出现无法安全自动裁决的数据破坏
  - `.75 / .74` 必须验证但节点失联且无可用 fallback
  - 本地上下文不足以继续，且任何合理 fallback 都无法验证
- 如果阻塞，必须写回：
  - `Blocked on`: 卡在哪
  - `Tried`: 已尝试过什么
  - `Why blocked`: 为什么这些尝试还不够
  - `Exact next step`: 下次恢复时第一步做什么
- 恢复执行时：
  - 先读完整个文档
  - 从最后一个未完成且未失效的步骤继续

## Status Writeback

- `Completed`:
  - 已完成 Team 结构化日志收口：
    - webhook fail / retry / dead-letter
    - JSONL 损坏读取告警
  - 已完成 Team sync / conflict 第二层视图模型：
    - `ConflictClass`
    - `SubjectLabel`
    - `Actions`
    - `StatusGroups`
    - `webhook_status`
  - 已完成 `TeamSyncMessage.Key()` 重复标准化收口，并补 benchmark：
    - `BenchmarkTeamSyncMessageKey`
  - 已完成强验证：
    - `go test ./internal/haonews/team -count=1`
    - `go test ./internal/plugins/haonewsteam -count=1`
    - `go test ./internal/haonews -run 'TestTeamPubSubRuntime|TestWriteTeamSyncState|TestTeam.*Conflict|TestTeam.*Ack' -count=1`
    - `go test ./internal/plugins/haonewsops -run 'TestPluginBuildServesBootstrapExplainAPI' -count=1`
    - `go build ./cmd/haonews`
  - 已完成本地运行态复核：
    - `/api/network/bootstrap` => `ready`
    - `/api/teams/archive-demo/sync` 已返回 `webhook_status`
- `Blocked`:
  - 无硬阻塞。
- `Next Step`:
  - 如继续，建议直接发布本轮 Team 收口批次：
    - `main`
    - `tag`
    - `release`
  - 或把同批次升级到 `.75 / .74` 后再做一次双节点可见面复核。
