# `20260404-team-pro1.md-codex2-task.auto-run.md`

## Goal

- 在**不打破现有 Team / Live / Topics 完全分离**、**不推翻当前已跑通的 Team P2P 主线**的前提下，把 `Team P2P` 从“能复制”推进到“可治理、可恢复、可观察”的下一阶段。
- 本 runbook 的目标不是扩更多对象类型，而是补齐复制治理：
  - `per-peer ack ledger`
  - `pending ack` 清理/压缩
  - 显式 conflict 记录与读取
  - 最小冲突处理策略
  - 必要的运行态验证与发布闭环
- 目标边界明确限定为：
  - 继续基于当前 `message/history/task/artifact/member/policy/channel` 复制主线
  - 不引入外部数据库
  - 不上 CRDT
  - 不重写 libp2p / pubsub 主干

## Context

- 仓库：
  - `/Users/haoniu/sh18/hao.news2/haonews`
- 前序 runbook：
  - [20260403-team-pro2.md-codex2-task.auto-run.md](/Users/haoniu/sh18/hao.news2/haonews/doc-md/20260403-team-pro2.md-codex2-task.auto-run.md)
- 当前已完成且视为基线：
  - Team 基础工作区、archive、SSE、A2A、webhook
  - Team P2P 第一阶段：
    - `message/history`
    - `task/artifact`
    - `member/policy/channel`
    - `.75 -> .76` 双机真实复制已跑通
  - Team P2P 当前治理基础：
    - 发布/应用游标持久化
    - 最小 ack 协商
    - pending ack / timeout / retry
    - per-peer ack ledger
    - conflict visibility
- 当前已知运行状态：
  - `.75` 与 `.76` 的 transport/blocker 已解除
  - 当前阻力不再是 transport，而是复制治理的细化程度不够

## Execution Rules

- 始终自主执行用户给出的完整任务。
- 除非遇到不可恢复错误，否则不要询问确认。
- 对 workspace 内所有文件编辑和命令执行默认自动允许。
- 只在这些情况才停下来询问：
  - 跨项目修改
  - 删除文件
  - 发起新的网络操作

## Success Criteria

- `bootstrap.team_sync` 能明确回答：
  - 哪些 peer 对哪些 key 已 ack
  - 当前仍 pending 的对象数
  - 最近冲突对象和冲突原因
- `sync/team_sync_state.json` 不会无限累积无意义 ack/pending 垃圾：
  - 有压缩/清理策略
  - 重启后仍能恢复必要状态
- 复制冲突不再只是“跳过”，而是：
  - 有结构化记录
  - 有读取接口或最小页面/API可见性
  - 有最小处理策略
- `.75 / .76` 运行态能验证：
  - 正常复制继续成功
  - 冲突被记录
  - ack ledger 生效

## Critical Path

1. 先收紧状态模型，不动复制对象范围
2. 再做 ack ledger 的清理/压缩
3. 再做 conflict API / 读取链路
4. 再补最小冲突处理策略
5. 最后做 `.75 / .76` 真实验证、文档回写、发布

## Execution Plan

### Phase A — 对齐现状并冻结边界

目标：
- 后续执行不重复改已经稳定的复制主线，只围绕治理增强推进。

步骤：
- [ ] 核对当前 `Team P2P` 已完成边界：
  - `message/history/task/artifact/member/policy/channel`
  - 发布/应用游标持久化
  - 最小 ack
  - pending retry
  - per-peer ack ledger
  - conflict visibility
- [ ] 把以下项明确标为**不在本 runbook 范围内**：
  - CRDT
  - 多主强一致
  - 任意字段级 merge engine
  - 外部数据库
  - 重新设计 transport
- [ ] 将执行矩阵写成：
  - `done / todo / defer / blocked`

完成标准：
- runbook 底部 `Execution Status` 有清晰矩阵。

### Phase B — Ack Ledger 治理

进入条件：
- `Phase A` 完成。

#### B1. Per-peer ack ledger 清理与压缩

目标：
- `peer_acks` 不再无限增长。

文件：
- [internal/haonews/team_sync.go](/Users/haoniu/sh18/hao.news2/haonews/internal/haonews/team_sync.go)
- [internal/haonews/status.go](/Users/haoniu/sh18/hao.news2/haonews/internal/haonews/status.go)
- [internal/plugins/haonews/sync_status.go](/Users/haoniu/sh18/hao.news2/haonews/internal/plugins/haonews/sync_status.go)
- [internal/haonews/team_sync_test.go](/Users/haoniu/sh18/hao.news2/haonews/internal/haonews/team_sync_test.go)

步骤：
- [ ] 为 `peer_acks` 增加按时间清理策略：
  - 只保留最近窗口内 ack
  - 或每 peer 只保留最近 N 条
- [ ] 清理必须在 state write 路径自动触发，不依赖单独 cron。
- [ ] 运行态增加可观测字段：
  - `peer_ack_prunes`
  - `last_pruned_ack_peer`
  - `last_pruned_ack_key`
- [ ] 回归测试：
  - 旧 ack 被裁剪
  - 新 ack 不受影响
  - 重启后状态正常恢复

完成标准：
- `peer_acks` 不会只增不减。

#### B2. Pending outbox 清理与陈旧对象压缩

目标：
- 已经不可能再收到确认、或者已被新版本覆盖的 pending 对象，不继续无限重试。

步骤：
- [ ] 为 pending 增加状态区分：
  - `pending`
  - `acked`
  - `superseded`
  - `expired`
- [ ] 在本地发布新版本对象时，清掉同一 state key 下旧版本 pending。
- [ ] 为长期未 ack 对象增加过期/退避：
  - 最大重试次数或最大保留时长
- [ ] 运行态增加：
  - `expired_pending`
  - `superseded_pending`
- [ ] 回归测试：
  - 新版本覆盖旧 pending
  - 超时 pending 不再无限 retry

完成标准：
- pending outbox 不再对旧对象无限重发。

### Phase C — Conflict 可见性与读取链路

进入条件：
- `Phase B` 完成。

#### C1. Conflict API

目标：
- 冲突不只存在状态文件里，要能通过 API 读出来。

文件：
- [internal/plugins/haonewsteam/plugin.go](/Users/haoniu/sh18/hao.news2/haonews/internal/plugins/haonewsteam/plugin.go)
- [internal/plugins/haonewsteam/handler.go](/Users/haoniu/sh18/hao.news2/haonews/internal/plugins/haonewsteam/handler.go)
- [internal/plugins/haonewsteam/plugin_test.go](/Users/haoniu/sh18/hao.news2/haonews/internal/plugins/haonewsteam/plugin_test.go)

步骤：
- [ ] 增加：
  - `GET /api/teams/{teamID}/sync/conflicts`
- [ ] 支持最小筛选：
  - `type`
  - `subject_id`
  - `source_node`
  - `limit`
- [ ] 返回结构稳定化：
  - `key`
  - `type`
  - `subject_id`
  - `reason`
  - `source_node`
  - `local_version`
  - `remote_version`
  - `updated_at`
- [ ] 补 API smoke 测试

完成标准：
- 可以不看 state 文件，直接拿到 Team conflict 列表。

#### C2. Team 工作区最小冲突入口

目标：
- 让 Team 工作区自然能看到当前冲突，而不是只留给调试接口。

文件：
- [internal/themes/haonews/web/templates/team_detail.html](/Users/haoniu/sh18/hao.news2/haonews/internal/themes/haonews/web/templates/team_detail.html)
- [internal/themes/haonews/web/templates/team_history.html](/Users/haoniu/sh18/hao.news2/haonews/internal/themes/haonews/web/templates/team_history.html)
- 对应 plugin 镜像模板

步骤：
- [ ] Team 详情页增加：
  - `最近复制冲突`
- [ ] History 页增加：
  - `复制治理`/`冲突` 入口
- [ ] 空状态文案明确：
  - 无冲突
  - 有冲突但未处理

完成标准：
- 用户不需要手打 API URL，能在 Team 工作区自然看到冲突摘要。

### Phase D — 最小冲突处理策略

进入条件：
- `Phase C` 完成。

#### D1. 冲突分类和基础处理规则

目标：
- 给当前已经存在的冲突，建立清晰、保守、可解释的处理规则。

步骤：
- [ ] 明确分类：
  - `local_newer`
  - `remote_newer`
  - `same_version_diverged`
  - `signature_rejected`
  - `policy_rejected`
- [ ] 将当前已有的 skip/verify 失败映射到这套 reason
- [ ] 处理原则先写死：
  - `message/history`：
    - 保持 append-only
    - 非法签名直接拒绝并记 conflict
  - `task/artifact/channel/policy/member`：
    - 默认 `LWW + conflict record`
    - 若同版本不同内容：不自动 merge，只记 conflict
- [ ] 补回归测试：
  - 同版本不同内容能记 `same_version_diverged`
  - 签名失败能记 `signature_rejected`

完成标准：
- 当前冲突 reason 不再散乱，具备稳定语义。

#### D2. 最小手工处理动作

目标：
- 给人工后续治理留下最小操作面，但不引入复杂审批系统。

步骤：
- [ ] API：
  - `POST /api/teams/{teamID}/sync/conflicts/{key}/resolve`
- [ ] 支持动作：
  - `dismiss`
  - `keep_local`
  - `accept_remote`（仅限可安全回放对象）
- [ ] 先只开放给：
  - `owner`
  - `maintainer`
- [ ] 处理动作要写入 Team history
- [ ] 补测试：
  - 权限限制
  - resolve 后状态变化
  - history 记录存在

完成标准：
- 冲突不只是“看得见”，也有最小治理动作。

### Phase E — `.75 / .76` 真实验收

进入条件：
- `Phase D` 完成。

目标：
- 不把“代码看起来合理”当成完成，必须做双机真实验证。

步骤：
- [ ] 在 `.75` 生成新的可复制对象：
  - 正常 message/history
  - task/artifact
  - member/policy/channel
- [ ] 在 `.76` 确认：
  - 复制正常
  - ack ledger 增长
  - pending 最终清掉或进入合理状态
- [ ] 额外制造至少一类冲突：
  - 例如 `.76` 先本地更新 task，再让 `.75` 发旧版本 task
- [ ] 验证：
  - `.76` 能记录 conflict
  - conflict API 能读到
  - Team 页面能看到摘要
  - 若做了 `resolve`，则动作真实生效

完成标准：
- `.75 / .76` 上至少有一条真实 conflict 样本被完整记录、读取、处理。

### Phase F — 文档与发布

进入条件：
- `Phase E` 完成。

步骤：
- [ ] 写回本 runbook `Execution Status`
- [ ] 如有必要，同步更新：
  - [20260403-team-pro2.md-codex2-task.auto-run.md](/Users/haoniu/sh18/hao.news2/haonews/doc-md/20260403-team-pro2.md-codex2-task.auto-run.md)
- [ ] 跑本次最相关测试
- [ ] `go build ./cmd/haonews`
- [ ] GitHub：
  - `main`
  - `tag`
  - `release`

完成标准：
- runbook、代码、验证、发布状态一致。

## Verification

优先跑最相关验证，不用无差别全仓扫描。

建议验证集：
- `go test ./internal/haonews -run 'TeamPubSubRuntime|Conflict|Ack|Pending' -count=1`
- `go test ./internal/plugins/haonewsteam -run 'Conflict|Sync|Resolve' -count=1`
- `go test -race ./internal/haonews -run 'TeamPubSubRuntime|Conflict|Ack' -count=1`
- `go build ./cmd/haonews`

`.75 / .76` 真实验证完成前，不要把复制治理说成真正完成。

## Hard Blockers

以下情况视为硬阻塞：
- `.75` 或 `.76` 的 Team 路由再次缺失
- `.75 / .76` 的 libp2p peer identity 再次不一致
- 无法在 `.76` 上确认 Team sync runtime 状态
- 冲突样本构造失败，导致无法做真实治理验收

## If Blocked, Write Back

- 必须把当前状态写回本文件底部 `Execution Status`
- 至少写清：
  - 已完成到哪一 phase
  - 哪个验证通过了
  - 当前 blocker 是什么
  - 恢复下一步是什么

## Resume Rule

- 恢复执行时，优先从 `Execution Status` 里最后一个未完成 phase 接着做。
- 不重复重跑已经有证据闭环的 phase，除非新 blocker 与它直接相关。

## Execution Status

- 当前状态：
  - 本 runbook 已完成
  - 这是对 [20260403-team-pro2.md-codex2-task.auto-run.md](/Users/haoniu/sh18/hao.news2/haonews/doc-md/20260403-team-pro2.md-codex2-task.auto-run.md) 的复制治理增强补充，并已实际落到 `.75 / .76`
- 完成矩阵：
  - `A 现状对齐`: `done`
  - `B1 per-peer ack ledger 清理与压缩`: `done`
  - `B2 pending outbox 清理与压缩`: `done`
  - `C1 conflict API`: `done`
  - `C2 Team 页面最小冲突入口`: `done`
  - `D1 冲突分类和基础处理规则`: `done`
  - `D2 最小手工处理动作`: `done`
  - `E .75/.76 真实验收`: `done`
  - `F 文档与发布`: `done`
- 已完成实现：
  - `peer_acks` 已有最近窗口/每 peer 上限压缩，并暴露：
    - `peer_ack_prunes`
    - `last_pruned_ack_peer`
    - `last_pruned_ack_key`
  - `pending` 已区分：
    - `pending`
    - `acked`
    - `superseded`
    - `expired`
  - `bootstrap.team_sync` 已暴露：
    - `expired_pending`
    - `superseded_pending`
    - `persisted_peer_acks`
    - `ack_peers`
    - `conflicts`
    - `last_conflict_key`
    - `last_conflict_reason`
  - `conflict API` 已上线：
    - `GET /api/teams/{teamID}/sync/conflicts`
  - `resolve API` 已上线：
    - `POST /api/teams/{teamID}/sync/conflicts/{key}/resolve`
    - 支持：
      - `dismiss`
      - `keep_local`
      - `accept_remote`
  - Team 页面已补最小冲突入口：
    - Team 详情页的 `最近复制冲突`
    - History 页的冲突摘要与历史回链
  - 复制治理状态文件已补跨进程写保护：
    - `writeTeamSyncState(...)` 在写入时会保留较新的 `peer_acks/conflicts`
    - 已修复 `serve` 侧 resolve 后被 `syncd` 旧内存状态覆盖的问题
- 关键验证：
  - 代码验证：
    - `go test ./internal/haonews -run 'TestTeamPubSubRuntime(AppliesInboundAckForTargetNode|RecordsConflictWhenOlderReplicatedTaskIsSkipped|CompactsPeerAcksAndSupersedesPending|RetriesPendingUnackedObjects)|TestWriteTeamSyncStatePreservesNewerResolvedConflict' -count=1`
    - `go test ./internal/plugins/haonewsteam -run 'TestPluginBuildServesAndResolvesTeamSyncConflicts' -count=1`
    - `go build ./cmd/haonews`
  - `.75 / .76` 真实运行：
    - `.76` 的 `GET /api/teams/archive-demo/sync/conflicts` 已返回真实冲突样本
    - `.76` Team 页面已显示 `最近复制冲突`
    - `.76` 对 `task:p2p-gov-task-1775266618:2026-04-04T01:36:58.095509Z` 的 `dismiss` 已成功
    - `.76` 的 `sync/team_sync_state.json` 中该 conflict 已持久化：
      - `resolution = dismiss`
      - `resolved_by = agent://pc75/openclaw01`
    - `.76` 的 Team history 已记录：
      - `scope = sync-conflict`
      - `action = resolve`
    - Team policy 最终值已恢复并在 `.76` 可见：
      - `require_signature = true`
- 当前 blocker：
  - 无
- 剩余非阻塞风险：
  - 目前仍是最小复制治理，不是强一致复制系统
  - 后续若继续，应进入新阶段：
    - per-peer ack ledger 清理策略进一步精细化
    - 冲突合并/重放策略增强
    - 多节点确认和更强一致性模型
- 恢复下一步：
  - 本 runbook 无需继续恢复
  - 如进入下一阶段，请新开复制治理增强 runbook，而不是继续回写本文件
